@model Nexawo.Application.test.Service1testViewModel
@{ 
 ViewData["Title"] = "test";
}

<div class=row>
<div class="col-md-4"></div>
		<div class=col-md-4>
			<form asp-action=Service1test>
				<div asp-validation-summary=ModelOnly class=text-danger></div>
				<div class=form-group>
					<label asp-for=First Name class=control-label></label>
					<input asp-for=First class=form-control />
					<span asp-validation-for=First Name class=text-danger></span>
				</div>
				<div class=form-group>
					<input type=submit value="Save" class="btn btn-primary" />
				</div>

		</form>
	</div>
</div>
